name = "start-stack-app"
enable_filestore = true
enable_gcs_fuse = false
enable_postgres = false
enable_mysql = false
enable_api_gateway = false
enable_secrets = false
enable_slack = false
enable_automated_deploys = false

[metadata]
  owner = "john.pham@applied.co"
  description = "start_stack command builder: web UI for building the truck launch command, with shared presets, closed loops and route autofill."
